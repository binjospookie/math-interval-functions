export const opened = /^\(([0-9-.]*),([0-9-.]*)\)$/;
export const closed = /^\[([0-9-.]*),([0-9-.]*)\]$/;
export const leftOpen = /^\(([0-9-.]*),([0-9-.]*)\]$/;
export const leftClosed = /^\[([0-9-.]*),([0-9-.]*)\)$/;

// console.log("[-123.4,-10)".match(leftClosed));
