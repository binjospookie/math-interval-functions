commit-msg:
  scripts:
    "template_checker":
      runner: bash
pre-commit:
  commands:
    prettier:
      run: npm run prettier
    lint:
      run: npm run lint
    test:
      run: npm t
pre-push:
  commands:
#    audit:
#      run: npm audit
